<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Conversor Dólar Blue</title>
  <style>
    body {
      padding: 16px;
    }
    .container {
      max-width: 500px;
      margin: auto;
    }
  </style>
</head>
<body>
  <h1>Conversor de Pesos a Dólar Blue</h1>
  <p>Introduce la cantidad en pesos y presiona "Convertir" para ver el equivalente en dólar blue.</p>

  <div class="container">
    <input type="number" id="pesos" placeholder="Cantidad en pesos">
    <button onclick="convert()">Convertir</button>
    <p id="resultado"></p>
  </div>

  <script>
    async function convert() {
      const pesos = document.getElementById('pesos').value;
      const response = await fetch('https://api.bluelytics.com.ar/v2/latest');
      const data = await response.json();
      const dolarBlue = data.blue.value_sell;
      const resultado = pesos / dolarBlue;
      document.getElementById('resultado').innerText = `Equivalente en dólar blue: $${resultado.toFixed(2)}`;
    }
  </script>
</body>
</html>
